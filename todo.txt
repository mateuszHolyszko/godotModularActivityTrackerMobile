SEE TODO IN elements

similar to program changed signal in program mnaager add session changed to session manager and hook it up to session menu

on singular datapoint deletion, refresh batch sizes

to exercise tie video (in workout exercise row give camera button that lets you record exrcise)

to exercise add half targets, like dip can hit triceps 0,5 times per set

in pick exercise include DisplayExerciseMods bellow exercise so we can make diff between same named exercise

For some menu (right now Init and Data{to be Exercise}) implement async loading with spinning loading indicator

Isometric exercises

Remove supersets from exercise entry

add data menu for data manipulation (perhaps we can transfer data to pc?)

in mesurment menu and to mesurment resource and to plotter2d, add "phases" like CUT or BULK, basicly we can add start_bulk and end_bulk with dates
than we could plot that on plotter 2d as str8 line on x axis

Settings menu

Make some script for elements so that when they change visibility they get tweened for visual effect (like for notes it would expand visually instead of appearing)

If session is active prohibit CRUDE operations (perhaps guard on manager level?)

In volume summary make query time input

### DONE ==============================================================================

#Presistent menu (namely for Session) 

#In bodyweight sets add last recorded body-weight to weight
#Dont allow for workout to start if no bodyweight can be found (perhaps notify user if last mesurment is old) # workout_menu.gd ready func
#Freestyle session thats uneditable (wont show up in Program menu)
#In init instead of carousel bellow 3d make user choose which 3d scene to display, and scroll in 360 sllowly, make possible to rotate with mouse
#to exercise add device type {dummbell, barbell, calisthenic, band, cabel, machine, jury-riged}, mainly for filtering
#to exercise add other modifiers, like incline, decline, seated, wraps, one arm, can be stacked, could be signified by icons, can reduce names lenght
#rename data menu to exercise menu,
