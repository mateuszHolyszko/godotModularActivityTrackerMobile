SEE TODO IN elements

In bodyweight sets add last recorded body-weight to weight
Dont allow for workout to start if no bodyweight can be found (perhaps notify user if last mesurment is old)

Isometric exercises

Remove supersets from exercise entry

Freestyle session thats uneditable (wont show up in Program menu)

rename data menu to exercise menu, add data menu for data manipulation (perhaps we can transfer data to pc?)

in mesurment menu and to mesurment resource and to plotter2d, add "phases" like CUT or BULK, basicly we can add start_bulk and end_bulk with dates
than we could plot that on plotter 2d as str8 line on x axis

Settings menu

Make some script for elements so that when they change visibility they get tweened for visual effect (like for notes it would expand visually instead of appearing)

For some menu (right now Init and Data{to be Exercise}) implement async loading with spinning loading indicator

If session is active prohibit CRUDE operations (perhaps guard on manager level?)

In volume summary make query time input

### DONE ==============================================================================

#Presistent menu (namely for Session) 
